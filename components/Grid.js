import React from 'react';
import PropTypes from 'prop-types';

const Grid = props => (
<div>
    <h1>Grid Component</h1>
</div>
);

Grid.defaultProps = {};

Grid.propTypes =  {};

export default Grid;